name: test & publish

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        sentencecase: [ 'off', 'on', 'on+guess' ]
        caseprotection: [ 'off', 'strict', 'as-needed' ]
    steps:
    - uses: actions/checkout@v2
    - run: echo "::set-output name=version::"`jq -r .version package.json`
      id: version
    - name: check for spurious build on tagged version
      run: echo "::set-output name=spurious::true"
      id: spurious
      if: github.event.head_commit.message == steps.version.outputs.version && !startsWith(github.ref, 'refs/tags/')
    - run: |
        echo commit message ${{ github.event.head_commit.message }}
        echo version ${{ steps.version.outputs.version }}
        echo spurious ${{ steps.spurious.outputs.spurious }}

    - name: install node
      if: steps.spurious.outputs.spurious != 'true'
      uses: actions/setup-node@v1
      with:
        node-version: 14.x
    - name: Cache node dependencies
      if: steps.spurious.outputs.spurious != 'true'
      uses: actions/cache@v2
      env:
        cache-name: cache-dependencies
      with:
        path: |
          ~/.npm
          ~/.pnpm-store
        key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-build-${{ env.cache-name }}-
          ${{ runner.os }}-build-
          ${{ runner.os }}-
    - name: install node dependencies
      if: steps.spurious.outputs.spurious != 'true'
      uses: pnpm/action-setup@v2
      with:
        version: latest
        run_install: true

    - run: npm test -- --sentence-case ${{ matrix.sentencecase }} --case-protection ${{ matrix.caseprotection }}
      if: steps.spurious.outputs.spurious != 'true'

  release:
    runs-on: ubuntu-latest
    needs: test
    steps:
    - uses: actions/checkout@v2
      if: startsWith(github.ref, 'refs/tags/')
    - name: install node
      if: startsWith(github.ref, 'refs/tags/')
      uses: actions/setup-node@v1
      with:
        node-version: 14.x
    - name: Cache node dependencies
      if: startsWith(github.ref, 'refs/tags/')
      uses: actions/cache@v2
      env:
        cache-name: cache-dependencies
      with:
        path: |
          ~/.npm
          ~/.pnpm-store
        key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-build-${{ env.cache-name }}-
          ${{ runner.os }}-build-
          ${{ runner.os }}-
    - name: install node dependencies
      if: startsWith(github.ref, 'refs/tags/')
      uses: pnpm/action-setup@v2
      with:
        version: latest
        run_install: true
    - name: Publish
      if: startsWith(github.ref, 'refs/tags/')
      uses: JS-DevTools/npm-publish@v1
      with:
        token: ${{ secrets.NPM_TOKEN }}
        access: public
