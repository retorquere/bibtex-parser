require('jest-specific-snapshot')
const parse = require('../parse')

const bibfile = <%= bibfile %>;
const options = <%= options %>;
const snapshot = <%= snapshot %>;

test(<%= name %>, () => {
  expect(parse(bibfile, options)).toMatchSpecificSnapshot(snapshot)
})
